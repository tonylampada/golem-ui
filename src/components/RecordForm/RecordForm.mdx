import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import { ConfigTable } from '../../../docs/ConfigTable'
import { AdapterTable, CodeBlock, DocBlocks, Prose } from '../../../docs/Prose'
import { recordFormConfigSchema } from './RecordForm.config'
import { recordFormDocs } from './RecordForm.docs'
import * as RecordFormStories from './RecordForm.stories'

<Meta of={RecordFormStories} name="Docs" />

# RecordForm

<a href="https://tonylampada.github.io/golem-ui/app/#/api/record-form" target="_top">
  The same page on a phone →
</a>

## What it is for

<Prose text={recordFormDocs.purpose} />

## The configuration object

<ConfigTable schema={recordFormConfigSchema} />

<DocBlocks blocks={recordFormDocs.configNotes} />

## The adapters it needs

<AdapterTable rows={recordFormDocs.adapters} />

<Prose text={recordFormDocs.adapterNotes} />

## One complete example

<CodeBlock code={recordFormDocs.example} />

<Canvas of={RecordFormStories.CreateOnAPhone} />

The same fields with `layout: 'two-column'` above 768px. The multiline note spans both columns:

<Canvas of={RecordFormStories.CreateTwoColumn} />

Edit mode loads ticket `j-4187` through `get` and fills every control from it. Change anything and
the bar says so; save and it writes only what changed:

<Canvas of={RecordFormStories.Edit} />

A store that refuses every write, naming `quote`. The sentence lands under that control, not at the
top of the page:

<Canvas of={RecordFormStories.AdapterRefuses} />

Fields the store owns, shown formatted with no control, and a delete that asks once:

<Canvas of={RecordFormStories.ReadOnlyFields} />
<Canvas of={RecordFormStories.DeleteWithConfirm} />

## Failure modes

<Prose text={recordFormDocs.failureModes} />

<Canvas of={RecordFormStories.InvalidConfig} />
