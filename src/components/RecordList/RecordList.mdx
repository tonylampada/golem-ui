import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import { ConfigTable } from '../../../docs/ConfigTable'
import { AdapterTable, CodeBlock, DocBlocks, Prose } from '../../../docs/Prose'
import { recordListConfigSchema } from './RecordList.config'
import { recordListDocs } from './RecordList.docs'
import * as RecordListStories from './RecordList.stories'

<Meta of={RecordListStories} name="Docs" />

# RecordList

<a href="https://tonylampada.github.io/golem-ui/app/#/api/record-list" target="_top">
  The same page on a phone →
</a>

## What it is for

<Prose text={recordListDocs.purpose} />

## The configuration object

<ConfigTable schema={recordListConfigSchema} />

<DocBlocks blocks={recordListDocs.configNotes} />

## The adapters it needs

<AdapterTable rows={recordListDocs.adapters} />

<Prose text={recordListDocs.adapterNotes} />

## One complete example

<CodeBlock code={recordListDocs.example} />

<Canvas of={RecordListStories.Table} />

The same config under 768px, which is where the table gives way to cards. The column headings go
with the table, so the sort becomes a control of its own above the list:

<Canvas of={RecordListStories.Cards} />

Chips, a search box and a page of three. Type `rock` and one row is left; `Load 3 more` fetches the
next page and appends it:

<Canvas of={RecordListStories.FiltersAndSearch} />

Nothing to show, the first page still in flight, and a row written by something else 2.5 seconds
after mount:

<Canvas of={RecordListStories.Empty} />
<Canvas of={RecordListStories.Loading} />
<Canvas of={RecordListStories.LiveUpdate} />

## Failure modes

<Prose text={recordListDocs.failureModes} />

<Canvas of={RecordListStories.InvalidConfig} />
