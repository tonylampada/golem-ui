import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import { ConfigTable } from '../../../docs/ConfigTable'
import { AdapterTable, CodeBlock, Prose } from '../../../docs/Prose'
import { chatConfigSchema } from './Chat.config'
import { chatDocs } from './Chat.docs'
import * as ChatStories from './Chat.stories'

<Meta of={ChatStories} name="Docs" />

# Chat

<a href="https://tonylampada.github.io/golem-ui/app/#/api/chat" target="_top">
  The same page on a phone →
</a>

## What it is for

<Prose text={chatDocs.purpose} />

## The configuration object

<ConfigTable schema={chatConfigSchema} />

## The adapters it needs

<AdapterTable rows={chatDocs.adapters} />

<Prose text={chatDocs.adapterNotes} />

## One complete example

<CodeBlock code={chatDocs.example} />

<Canvas of={ChatStories.Conversation} />

Send something in this one and the answer streams in word by word, on the kit's fake adapter:

<Canvas of={ChatStories.Live} />

Empty, mid-stream, and with timestamps turned on:

<Canvas of={ChatStories.Empty} />
<Canvas of={ChatStories.Streaming} />
<Canvas of={ChatStories.WithTimestamps} />

## Failure modes

<Prose text={chatDocs.failureModes} />

<Canvas of={ChatStories.InvalidConfig} />
