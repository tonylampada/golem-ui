import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import { ConfigTable } from '../../../docs/ConfigTable'
import { AdapterTable, CodeBlock, DocBlocks, Prose } from '../../../docs/Prose'
import { authConfigSchema } from './Auth.config'
import { authDocs } from './Auth.docs'
import * as AuthStories from './Auth.stories'

<Meta of={AuthStories} name="Docs" />

# Auth

<a href="https://tonylampada.github.io/golem-ui/app/#/api/auth" target="_top">
  The same page on a phone →
</a>

## What it is for

<Prose text={authDocs.purpose} />

## The configuration object

<ConfigTable schema={authConfigSchema} />

<DocBlocks blocks={authDocs.configNotes} />

## The adapters it needs

<AdapterTable rows={authDocs.adapters} />

<Prose text={authDocs.adapterNotes} />

## One complete example

<CodeBlock code={authDocs.example} />

<Canvas of={AuthStories.SignedOutPassword} />

A one-time code instead of a password, and the same screen opened on an invite link:

<Canvas of={AuthStories.SignedOutCode} />
<Canvas of={AuthStories.SignUpViaInvite} />

The member list, seen by a role that manages and by one that does not:

<Canvas of={AuthStories.MembersAsAdmin} />
<Canvas of={AuthStories.MembersAsMember} />

The account menu, and a guard turning the wrong role away:

<Canvas of={AuthStories.AccountMenu} />
<Canvas of={AuthStories.GuardDenies} />

## Failure modes

<Prose text={authDocs.failureModes} />

<Canvas of={AuthStories.InvalidConfig} />
