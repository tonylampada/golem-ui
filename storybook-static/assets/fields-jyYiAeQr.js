import{s as e,_ as i,o as a,b as t,u as r,n as o,a as l}from"./emitter-Dbdymuz5.js";const n=["text","number","date","datetime","enum","boolean","money","user"],s={key:e().min(1).describe("The property to read off each record. Must match what the adapter returns."),label:e().min(1).describe("What names the field on screen: a column heading, or the label above a control."),type:i(n,{error:d=>`Unknown field type ${JSON.stringify(d.input)}. Pick one of: ${n.join(", ")}.`}).describe("How the value is rendered and edited. `text` verbatim; `number` grouped; `date` and `datetime` as UTC calendar dates; `enum` as a coloured chip or a select; `boolean` as Yes/No or a checkbox; `money` as a currency amount; `user` as a name with initials."),format:e().optional().describe("A hint read only by some types: an ISO currency code for `money` (default USD), an `Intl` dateStyle — short, medium, long, full — for `date` and `datetime`, and a digit count for `number`. Ignored by every other type.")},m=a({...s,primary:t().default(!1).describe("Marks the field that titles a row: the first column of the table and the heading of a card. At most one field may set it; with none, the first field is the title.")}).strict(),c=a({id:e().min(1).describe("The value written to the record when this choice is picked."),label:e().min(1).describe("What the reader sees in the control. Never written back.")}).strict(),b=a({...s,required:t().default(!1).describe("Blocks submit while the value is empty, and marks the label with an asterisk."),readOnly:t().default(!1).describe("Shows the value formatted, with no control and no place in the patch. Use it for what the server owns — an id, a created date, a computed total."),help:e().optional().describe("One line under the control, always visible. Say what a good value looks like."),placeholder:e().optional().describe("Ghost text inside an empty control. A sample value, never a second label."),options:l(c).min(1).optional().describe("The choices for an `enum` field, as `{ id, label }`. The reader picks the label and the record gets the id. Required on `enum`, rejected on every other type."),min:r([o(),e()]).optional().describe("The lowest accepted value: a number for `number` and `money`, an ISO date or datetime string for `date` and `datetime`. Ignored by every other type."),max:r([o(),e()]).optional().describe("The highest accepted value, in the same shape as `min`."),multiline:t().default(!1).describe("Draws a `text` field as a textarea, spanning both columns of a two-column layout. Ignored by every other type."),users:i(["members","free"]).default("members").describe("Where a `user` field gets its choices: `members` is a select over `Identity.listMembers()`, `free` is a text box for a name typed by hand. Ignored by every other type.")}).strict();export{b as f,m as l};
